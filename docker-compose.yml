version: '3.8'

services:
  redis:
    image: redis/redis-stack:latest
    container_name: trending-engine-redis
    ports:
      - "6380:6379"
      - "8002:8001" # Redis Insight UI
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
